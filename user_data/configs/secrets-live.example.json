{
    "_comment": "範本檔案,複製為 secrets-live.json(不進 git)後填入實際值。金鑰務必依 security-policy.md 第 1 節規範建立(僅交易、禁止提幣、IP 白名單)。",
    "exchange": {
        "key": "YOUR_LIVE_API_KEY",
        "secret": "YOUR_LIVE_API_SECRET"
    }
}
